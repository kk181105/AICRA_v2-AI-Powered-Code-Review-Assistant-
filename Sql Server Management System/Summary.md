# Database (SQL Server)

This folder contains the SQL Server database scripts used for the AI Code Review & Learning Assistant project i.e i'ts 3 AI Agents (Context, Predictor, Fragility Server). The scripts include database creation, table definitions, relationships, and sample queries required for the application. The database stores user information, code submissions, review results, and other application data, enabling seamless integration between the Flask backend and SQL Server.
